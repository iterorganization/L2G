[build-system]
requires = [
    "setuptools",
    "Cython>=0.29.21",
    "matplotlib>=3.3.0",
    "numpy>=1.19.5",
    "scipy>=1.5.4",
    "wheel",
]
optional = ["vtk", "medcoupling"] # For I/O
build-backend = "setuptools.build_meta"
